<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.MelomanosVinil" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/background_gradient</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.MelomanosVinil" parent="Base.Theme.MelomanosVinil" />

    <style name="Theme.Vinilos" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary</item>
        <item name="colorAccent">@color/color_primary</item>
        <item name="android:windowBackground">@color/screen_background_purple</item>

        <!-- Atributos específicos para Lollipop -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/color_primary</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/color_primary</item>
        <item name="android:colorControlNormal">@color/color_primary</item>
        <item name="android:colorControlActivated">@color/color_primary</item>
        <item name="android:colorControlHighlight">@color/color_unpressed</item>

        <!-- Garantizar colores consistentes en diálogos -->
        <item name="dialogCornerRadius">12dp</item>
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
    </style>

    <style name="AlertDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="buttonBarPositiveButtonStyle">@style/CustomButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/CustomButtonStyle</item>
        <item name="android:background">@color/color_secondary</item>
        <item name="android:textColorPrimary">@color/color_primary</item>
    </style>
</resources>